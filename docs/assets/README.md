# README assets

The adaptive icon files are presentation copies of the official ChatBotKit mark in `Resources/Brand/icon.svg`. The upstream source and pinned revision are recorded in `Resources/Brand/README.md`.

The approved product image should be added as `studio-screenshot.png`. The root README already contains the matching image block; remove its surrounding HTML comment after adding the file.
